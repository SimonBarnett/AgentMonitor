# Build/test plan: shortcut windows on/off (#18)

1. Read issue #18. Add `-Windows on|off` (default `on`) and `.cmd` `on`/`off` parsing.
2. One-click launchers pass `on`. Document in README and operator-playbook.
3. Open PR linking #18. Do not stamp UAT.
