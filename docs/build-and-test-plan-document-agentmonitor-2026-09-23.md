# Build/test plan: Document AgentMonitor

1. Read this FR + `Watch-AgentHealth.ps1` header and launchers on pulled main.
2. Write `docs/` operator document from LOCKED lines. Expand `README.md`
   to point at it. Do not invent Desktop paths or tokens.
3. Open PR linking the issue. Do not stamp UAT.
